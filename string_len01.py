from re import X


def main(a):
    """
    A variable of type string is given. Find its length.
    Args:
        a: string
    Returns:
        length of string
    """
    y = len(a)
    return y 
x=main('ssd dgd')
print(x)